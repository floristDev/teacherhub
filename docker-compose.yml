version: '3.9'

# =============================================================================
# TeacherHub — Local Development Services
# =============================================================================
# Start with: docker compose up -d
# Stop with:  docker compose down
# Reset DB:   docker compose down -v && docker compose up -d

services:
  postgres:
    image: postgres:16-alpine
    container_name: teacherhub-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: teacherhub
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d teacherhub']
      interval: 10s
      timeout: 5s
      retries: 5

  # Uncomment to add Redis for caching / rate limiting
  # redis:
  #   image: redis:7-alpine
  #   container_name: teacherhub-redis
  #   restart: unless-stopped
  #   ports:
  #     - '6379:6379'
  #   volumes:
  #     - redis_data:/data
  #   healthcheck:
  #     test: ['CMD', 'redis-cli', 'ping']
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

volumes:
  postgres_data:
    driver: local
  # redis_data:
  #   driver: local
